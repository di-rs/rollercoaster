---
"@di-rs/rollercoaster": minor
---

Migrate to standalone executables with bun build --compile. Package now ships as JS bundle for npm install, plus standalone executables for GitHub releases and Homebrew (no Node.js required).
